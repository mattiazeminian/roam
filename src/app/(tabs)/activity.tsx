/**
 * Activity is the primary-tab entry point for the existing History screen.
 * Keeping the implementation in `src/app/history.tsx` preserves the current
 * activity UI and its direct route while the tab adopts the product language.
 */
export { default } from '../history';
